const token = `BOT_TOKEN`
const botID = `BOT_ID`
const support = `ADMIN_USERNAME`
const botUname = `BOT_USERNAME`
const adminBot = `YOUR_TELEGRAM_ID`

const variable = {
    token,
    botID,
    support,
    botUname,
    adminBot
}

module.exports = variable